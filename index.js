// add solution here
function theBeatlesPlay(musicians, instruments)
{
  var array = [];
  for (var j=0; i < musicians.length; i++)
  {
    array[j] = musicians[j] + " plays " + instruments[j]
  }
  return array;
}